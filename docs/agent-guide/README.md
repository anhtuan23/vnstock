# Vnstock Agent Guide

This directory contains only the free-tier `vnstock` portion of the external
agent guide from https://github.com/vnstock-hq/vnstock-agent-guide.

Future agents should use `vnstock/` for public API examples and usage guidance
for this local fork. Paid-tier ecosystem guides are intentionally omitted
because this workspace does not have access to those libraries.

